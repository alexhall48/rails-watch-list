<h1>Show list</h1>

<h4><%= @list.name %></h4>

<ul>
<% @list.bookmarks.each do |bookmark| %>
<li><%= bookmark.movie.title %> <%= link_to 'Delete', bookmark_path(bookmark), method: :delete, class: "btn btn-warning mt-3", data: { confirm: 'Are you sure?' } %>
</li>
<% end %>
</ul>

 <%= link_to 'Go back', lists_path, class: "btn btn-danger mt-3" %>

 <%= link_to 'Add a Bookmark', new_list_bookmark_path(@list), class: "btn btn-primary mt-3" %>

<%= link_to 'Delete', bookmark_path(), method: :delete, class: "btn btn-warning mt-3", data: { confirm: 'Are you sure?' } %>
